<div class="row mb-5">
  <div class="col d-flex">
    <%= image_tag 'stethoscope.svg', class: 'me-2 h-100' %>
    <h4 class="h4 mb-0 lh-base">My Doctors</h4>
  </div>
</div>

<div id="doctors" class="grid gap-2">
  <% @doctors.each do |doctor| %>
    <%= render doctor %>
  <% end %>
</div>
